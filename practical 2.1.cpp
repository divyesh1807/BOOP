#include <iostream>
int main()
{
    int a,b,c;
    std::cout<<"enter the two numbers:";
    std::cin>>a>>b;
    std::cout<<"the addition between two number:"<<a+b;
    std::cout<<"the difference between two number:"<<a-b;
    std::cout<<"the multiple between two number:"<<a*b;
    std::cout<<"the division between two number:"<<a/b;
    return 0;
}